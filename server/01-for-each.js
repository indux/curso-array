const letters = ["a", "b", "c"];

// FOR

for (let index = 0; index < letters.length; index++) {
  const letter = letters[index];
  console.log(letter);
}

// FOR EACH

letters.forEach((letter) => {
  console.log(letter);
});
